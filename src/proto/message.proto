syntax = "proto3";

package demo;

service MessageService {
  // 服务器流式推送消息给客户端
  rpc StreamMessages(ClientInfo) returns (stream Message);
}

message ClientInfo {
  string client_name = 1;
}

message Message {
  string content = 1;
  int64 timestamp = 2;
}
